# WebSocket (PartyKit) host the client connects to.
# Local dev: leave unset — it defaults to localhost:1999 (npm run party).
# Production: set to your deployed PartyKit host before `npm run build`.
# VITE_PARTY_HOST=brawler.YOURNAME.partykit.dev
